<!DOCTYPE html>
<html lang="fr">

<head>
    <title>{% block title %}Accueil{% endblock %} | Mon super site</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" href="https://www.cegepgarneau.ca/favicon.ico" />
    <link rel="stylesheet" href="{{ url_for('static', filename='css/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/styles.css') }}">
</head>

<body>
    <header>
        <div class="container">
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <div class="container-fluid">
                    <a class="navbar-brand" href="{{ url_for('index') }}">Mon Site</a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <ul class="navbar-nav ms-auto">

                            {% if session.get('id_utilisateur') %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ url_for('services.ajouter_service') }}">Ajouter service</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ url_for('services.liste') }}">Liste services</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ url_for('comptes.Espace_utilisateur') }}">Espace
                                    utilisateur</a>
                            </li>

                            {% if session.get('est_admin') %}
                            <li class="nav-item">
                                <a class="nav-link fw-bold text-danger" href="{{ url_for('comptes.admin') }}">Admin</a>
                            </li>
                            {% endif %}

                            <li class="nav-item">
                                <a class="nav-link" href="{{ url_for('comptes.deconnexion') }}">Déconnexion</a>
                            </li>
                            {% else %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ url_for('comptes.connexion') }}">Se connecter</a>
                            </li>
                            {% endif %}

                        </ul>
                    </div>
                </div>
            </nav>
        </div>
    </header>


    <main class="container mt-4">
        {% block contenu %}
        {% endblock %}
    </main>

    <footer class="text-center mt-4 py-3 border-top">
        <p>Tchamdja Milca & Josiane Manzi &copy; 2025</p>
    </footer>
</body>

</html>